# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
.output
.vinxi
.tanstack/**
.nitro
*.local

# Wrangler / Cloudflare
.wrangler/
.dev.vars

# NOTE: .env é versionado de propósito — o build do Vite no Lovable depende
# dele para as variáveis VITE_ em tempo de build. Ele contém apenas a anon/
# publishable key (pública, já vai no bundle do client) e a URL do projeto,
# sem service-role. Não adicionar segredos reais aqui.
.env.local
.env.*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

test-results/
scratch/
*.code-workspace
playwright-report/
